%table#channel_items
  - page.items.each do |item|
    %tr
      %td
        =item.text
      %td
        %a{ :href => item.link }="Today"
      %td
        -if item.archive_link
          %a{ :href => item.archive_link }="Archive"
